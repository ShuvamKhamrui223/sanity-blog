const CategoryHeader = ({
  category = "all categories",
}: {
  category: string;
}) => {
  return (
    <section className="max-w-content-max-width mx-auto px-page-margin mb-stack-lg fade-in">
    </section>
  );
};

export default CategoryHeader;
